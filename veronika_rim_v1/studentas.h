#pragma Once
#include <string>
#include <vector>

struct Studentas{
    std:string vard;
    std:string pav;
    std:vector<int> nd;
    int egzas
};

double skaiciuoti_vidurki(const std::vector<int>& nd);
double skaiciuoti_mediana(std::vector<int> nd);
